<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sopt.saver.saver">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".application.ApplicationController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".Category.CategoryActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Mypage.MyPageActivity" />
        <activity android:name=".Electronics.ERecyclerViewActivity" />
        <activity android:name=".Electronics.ESellerRecyclerViewActivity" />
        <activity android:name=".Mydeal.MydealRecyclerViewActivity" />
        <activity android:name=".Question.QuestionActivity" />
        <activity android:name=".Message.MessageActivity" />
        <activity android:name=".DetailInfo.DetailActivity" />
        <activity android:name=".Sign.SignUpActivity" />
        <activity android:name=".Answer.AnswerActivity" />
        <activity android:name=".Electronics.ESellerListViewActivity"></activity>
    </application>

</manifest>